# Todo

- opening multiple files
  - when in `Temp` mode, check if there is another instance of the music player running in `Temp` mode, and add the song to that one and then close itself
- faster skipping
  - allow for any skipping during song
- mp4 implementation fix (it just closes the program)
- proper version numbers maybe?
